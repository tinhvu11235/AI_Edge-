[project]
name = "ai-edge-assistant"
version = "0.1.0"
description = "Always-on AI Edge assistant pipeline for Raspberry Pi benchmark"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
