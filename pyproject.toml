[project]
name = "edge-voice-ptt-test"
version = "0.1.0"
description = "Simulated benchmark harness for Raspberry Pi 5 voice-to-voice PTT AI Edge test"
requires-python = ">=3.10"
dependencies = ["numpy>=1.24", "psutil>=5.9"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
